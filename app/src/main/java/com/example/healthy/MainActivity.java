package com.example.healthy;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.Button;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }

    private void initSaveBotton()
    {
        Button saveButton = (Button) findViewById(R.id.Add){
            saveButton.setOnClickListeners(new onCreatePanelView().setOnClickListener();
        {@override
       public void onclick (view v)
            {healthyDataSource hd= new healthyDataSource(MainActivity.this);
            hd.open();
            boolean success = false;
            if(

            )
    }
    }
}
